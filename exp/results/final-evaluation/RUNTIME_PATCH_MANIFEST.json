{
 "recorded_utc": "2026-08-23T09:59:59.942950+00:00",
 "previous_runtime_freeze": {
  "commit": "88f54f9",
  "patch_blob": "c939cd311f6057b28ba21a76d9ac010e83dc152a"
 },
 "change": "observation-only Algorithm 2 logging, to separate b1 from b2",
 "b_root_cause": "_evict_all_waiting_requests released the staged list only after an early return that an empty waiting queue took, so a staged request survived a deactivation and its sequence was never retired. The release and its report now run before any return; the Redis drain the early return exists for is unchanged.",
 "a_identity": "The ordinary BackendAdmitReq tagged itself with self.model_name, which follows the reused worker slot rather than the request. It now carries the model Algorithm 2 scheduled the request under, matching what 84f880f already does for adoption grants.",
 "explicitly_unchanged": [
  "tau",
  "frozen c_i",
  "Moore-Hodgson select()",
  "deadline and arrival semantics",
  "migration policy",
  "activation policy",
  "KV allocator",
  "workload",
  "SLOs",
  "admission ordering semantics"
 ],
 "c_i_reuse": {
  "decision": "keep the frozen c_i; stage 01 is NOT re-run",
  "why": "The c_i profiling arm runs a solo server with no --enable-gpu-scheduler, --enable-controller or --enable-moore-hodgson, so _alg2_runtime_gate is False and _alg2_pending_adoption is never populated; a solo profile performs no deactivation, so the verdict handler, the worker-slot deferral and the placeholder discard are all unreachable. The drain-order swap is in the GPU scheduler process, which that arm does not run.",
  "audited_utc": "2026-08-23T05:07:17.742678+00:00"
 },
 "new_runtime_patch_blob": "dd92b92110ccd905ba55281011d2d9fb392d2ebd",
 "patch_blob": "caef46638c450dbd64fc9f3f63b19235dfe839d2",
 "ownership_invariant": "A dispatched Algorithm 2 sequence is owned by the GPU scheduler until the backend acknowledges admission; after that acknowledgement ownership passes to the engine-side lifecycle accounting.",
 "dispatch_seq_ownership": {
  "root_cause": "A sequence issued at dispatch for a request the engine never fetched was invisible to every retirement path: the engine cannot report a request it never held, and the backend drain found an empty queue. GPU 0's frontier froze on seq 1159 (model_4#140) for the rest of the run while the request itself was re-dispatched on GPU 1 as seq 2110 and served.",
  "fix": "On a model departure the GPU scheduler retires the sequences it still owns for that model -- dispatched, not yet acknowledged -- through the existing _mh_retired_admit_seqs / _mh_retired_start_seqs and the existing contiguous-prefix drain. Already-acknowledged requests are untouched and remain the engine-side lifecycle's responsibility.",
  "race_handling": "An acknowledgement, prefill start or completion still in flight when the sequence was retired names a tombstoned rid: it is recognised as already accounted exactly once, logged, and moves no frontier. It is not an ordering violation. A tombstone is cleared when the same rid is dispatched again on that GPU, so a returning request is always governed by its live record.",
  "not_added": [
   "timeout",
   "seq skip",
   "bypass",
   "ordering relaxation",
   "heuristic",
   "magic constant"
  ],
  "all_departure_reasons_verified": [
   "kv-stash",
   "staged-never-admitted",
   "evicted-to-frontend",
   "backend-queue-drain"
  ],
  "call_site_audit": "Every MigratedAwayReq originates from the deactivate handler (scheduler.py:1993 and :2055 call _evict_all_waiting_requests; _drain_backend_queue is reached only from it; the kv-stash report is inside _v6_stash_captured). There is no path that reports a request as gone while its model stays resident, so the sweep can never retire a live sequence of a resident model."
 },
 "deactivation_rollback": {
  "invariant": "A deactivation that rolls back leaves the source operational at every layer, and leaves ownership where it was.",
  "classification": "all changes CORRECTNESS-PLUMBING; none PAPER, none NEW-POLICY",
  "changes": [
   "gpu_scheduler._recv_requests_loop drains the engine channel before the request handler. Every engine-channel message (ActivateReqOutput, DeactivateReqOutput, BatchRunReq, BackendAdmitReq, PrefillCompleteReq, MigratedAwayReq, AdoptResumedReq) reports on work the scheduler itself forwarded, so none can depend on a pending control message; the reverse dependency is real.",
   "worker_pool.handle_deactivate_model no longer releases the slot; gpu_scheduler._handle_deactivate_result releases it on the success verdict.",
   "The DeactivateReqInput branch no longer pops the queue or the backend queue; both move to the success verdict, so a rollback has nothing to undo.",
   "On a failed verdict the model state returns to activated -- it was left at deactivating, which is excluded from frontend intake and from admission.",
   "alg2_resumed placeholders are discarded on success instead of being returned to the frontend as if they were requests.",
   "_alg2_release_pending_adoption delivers held payloads to the frontend itself and is called on P0/P1/P3/P4; the P2 rollback simply does not call it."
  ],
  "no_retry_trigger": "_alg2_request_adoption keeps its single caller. On P2 the placeholder is never popped, so the outstanding ask stays valid and the existing Prism scheduler resumes the adoption unaided.",
  "not_added": [
   "timeout",
   "retry",
   "heuristic",
   "duplicate guard",
   "forced slot reclaim",
   "double assignment"
  ],
  "unchanged": [
   "tau",
   "frozen c_i",
   "Moore-Hodgson select()",
   "Algorithm 1",
   "Algorithm 2 ordering",
   "deadline and arrival semantics",
   "migration policy",
   "activation policy",
   "KV allocator",
   "workload",
   "SLOs",
   "admission ordering"
  ],
  "path_table": {
   "P0 already-deactivated": {
    "owner": "frontend",
    "frontend_deliveries": 1
   },
   "P1 eviction requested": {
    "owner": "frontend",
    "frontend_deliveries": 1
   },
   "P2 stash rollback": {
    "owner": "source engine",
    "frontend_deliveries": 0
   },
   "P3 V6 eviction": {
    "owner": "frontend",
    "frontend_deliveries": 1
   },
   "P4 V6 off": {
    "owner": "frontend",
    "frontend_deliveries": 1
   }
  }
 },
 "staged_request_return": {
  "invariant": "Before the source gives up ownership, the request payload is returned to the frontend exactly once; only then is the source-local Algorithm 2 sequence retired.",
  "root_cause": "_evict_all_waiting_requests released the staged list into the ledger only. _alg2_report_migrated_away tells the GPU scheduler to stop waiting for a sequence; it delivers the request nowhere. So model_2#58 had sequence 1014 retired and both frontiers drained over it while the request itself was discarded, and cal-0p07-s0 sat at 8,226 of 8,227 with both GPUs idle.",
  "fix": "Each staged request is returned through the existing frontend conversion path before the list is cleared, then the existing staged-never-admitted report retires the sequence -- the same order the waiting-queue and backend-drain paths already use.",
  "scope": "the staged cleanup inside _evict_all_waiting_requests only",
  "not_added": [
   "retry",
   "timeout",
   "duplicate guard",
   "scheduling policy",
   "ordering change"
  ],
  "failure_handling": "A request that cannot be serialised back is logged as staged_return_failed and still reported, matching the waiting-queue path: withholding the retirement would block the frontier on a request that has left this GPU either way."
 },
 "alg2_observation": {
  "purpose": "Separate two hypotheses for the GPU0 3113-3115 stall: the engine loop running with a shared token that never matched (b1), against the loop not running at all (b2). The artifacts could not tell them apart -- staging is not logged and an activated idle engine is as silent as a dead one.",
  "added": [
   "_alg2_obs_fetch: every request taken off backend:<model>, with rid, alg2_seq, the shared token read at that moment, and waiting/running/staged/_activated",
   "_alg2_obs_no_promote: staged work exists and none matched the token -- staged head rid/seq, the staged sequences, expected and shared token, counts. Rate-limited to 5 s.",
   "_alg2_obs_heartbeat: emitted from the event loop every 5 s whether or not the engine is activated and whether or not it has work"
  ],
  "log_marker": "[PAPER-ALG2-OBS]",
  "log_location": "<run>/server-logs/server.log",
  "behaviour_change": "none. The observers only read state and log; they take no locks, write nothing, and every one is wrapped so it cannot raise. One assignment was added -- fetched_reqs is initialised to [] before the max_fetch_count branch -- so the observer can be called on a path where it was previously unbound; the branch itself is unchanged.",
  "not_added": [
   "timeout",
   "retry",
   "heuristic",
   "recovery",
   "admission or ordering change"
  ],
  "verdict_rule": {
   "b1": "heartbeats continue and staged_not_promoted records the mismatch",
   "b2": "heartbeats stop and no fetch is recorded"
  }
 }
}