{
  "recorded_utc": "2026-08-23T01:14:46.884076+00:00",
  "previous_runtime_freeze": {
    "commit": "8cb8e7a",
    "patch_blob": "0734d67ab932aa8f3746c0d2584a505fc9fe3276"
  },
  "change": "A+B request lifecycle / accounting identity patch only",
  "b_root_cause": "_evict_all_waiting_requests released the staged list only after an early return that an empty waiting queue took, so a staged request survived a deactivation and its sequence was never retired. The release and its report now run before any return; the Redis drain the early return exists for is unchanged.",
  "a_identity": "The ordinary BackendAdmitReq tagged itself with self.model_name, which follows the reused worker slot rather than the request. It now carries the model Algorithm 2 scheduled the request under, matching what 84f880f already does for adoption grants.",
  "explicitly_unchanged": [
    "tau",
    "frozen c_i",
    "Moore-Hodgson select()",
    "deadline and arrival semantics",
    "migration policy",
    "activation policy",
    "KV allocator",
    "workload",
    "SLOs",
    "admission ordering semantics"
  ],
  "c_i_reuse": {
    "decision": "keep the frozen c_i; stage 01 is not re-run",
    "why": "The c_i profiling arm launches a solo server without --enable-gpu-scheduler, --enable-controller or --enable-moore-hodgson, so _alg2_runtime_gate is False: the staged list is never populated, BackendAdmitReq is never sent, and a solo profile performs no deactivation, so _evict_all_waiting_requests is never reached. Neither symbol appears anywhere in profile_v2.py or final_profile_ci.sh. The patch cannot reach the prefill-throughput measurement.",
    "audited_symbols": [
      "_evict_all_waiting_requests",
      "_alg2_staged_generation_reqs",
      "BackendAdmitReq",
      "process_input_gen_requests"
    ],
    "frozen_c_i_file": "exp/results/final-evaluation/01-ci-profile/prefill_speed_final_a100.json"
  },
  "new_runtime_patch_blob": "dd92b92110ccd905ba55281011d2d9fb392d2ebd"
}
